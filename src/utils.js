
export const constants = Object.freeze({
    /* UPDATE THE STYLES(canvas.styles.css), if the width, height is altered here */ 
    SCROLLABLE_CANVAS_WIDTH: 2000,
    SCROLLABLE_CANVAS_HEIGHT: 1000,
    
    SCROLL_SENSITIVITY: 0.8,
})